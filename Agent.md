# 项目开发说明

## Katana 二次开发环境

- 本项目根目录下的 `katana/` 是当前使用的 Katana 二次开发环境。涉及 Katana 的功能调整或问题修复，应直接在该目录中进行，不要额外引入源码副本。
- `katana/` 拥有独立的 `.git`，是单独管理版本的 Git 仓库；主项目的 Git 操作不会自动包含该目录中的改动。
- 当主项目的变更涉及 Katana 时，可以同步更新 `katana/` 中的相关实现、测试和文档，并检查主项目的依赖配置是否与之保持一致。
- 检查或提交变更时，应分别查看主项目和 Katana 仓库的状态与差异，例如使用 `git -C katana status` 和 `git -C katana diff` 检查 Katana 改动。
